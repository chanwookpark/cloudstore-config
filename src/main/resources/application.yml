info:
  component: Config Server

spring:
  application:
    name: configServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chanwookpark/cloudstore-config-repo

endpoints:
  restart:
    enabled: true
